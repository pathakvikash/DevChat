import React from 'react';
import ChatApp from '@/components/ChatApp';
const Home = () => {
  return (
    <div className={`flex h-[100vh] `}>
      <ChatApp />
    </div>
  );
};

export default Home;
